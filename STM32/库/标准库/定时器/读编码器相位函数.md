需要[[配置编码器接口]]
```
//读编码器相位
//强制把CNT的无符号转成了有符号数
int16_t Encoder_Get(void)
{
	return TIM_GetCounter(TIM3);
}

```

```
//读编码器相位并复位
//强制把CNT的无符号转成了有符号数
int16_t Encoder_Get(void)
{
	int16_t Temp;
	Temp = TIM_GetCounter(TIM3);
	TIM_SetCounter(TIM3, 0);
	return Temp;
}
```