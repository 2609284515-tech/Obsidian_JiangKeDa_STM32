名字在startup里找IRQHandler结尾
中断函数是不用声明的，自动执行
一般来说要搭配[[返回函数]]或者extern变量或者直接把中断函数复制到变量所在文件，这样能避免在中断函数里操作硬件，与main冲突
不能写太长的延时，那样会丢失信息

形如
```
void xx_IRQHandler(void)
{
	if (xx_GetITStatus(...) == SET)
	{
		
		xx_ClearITPendingBit(...);
	}
}
```
