需要在[[Init函数]]里[[改变电平]]为初始状态
需要[[GPIO初始化]]
```
GPIO_SetBits(GPIOC, GPIO_Pin_13);
GPIO_ResetBits(GPIOC, GPIO_Pin_13);
GPIO_WriteBit(GPIOA, GPIO_Pin_0,Bit_RESET);
	//用1和0要用(BitAction)转换
GPIO_Write(GPIOA, ~0x0001);
	//~是取反，16位二进制从低位到高位对应PA0~15
```
